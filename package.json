{
  "name": "zabbix-max-alert-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Zabbix webhook integration for sending alerts to MAX messenger",
  "scripts": {
    "test": "node --test",
    "verify": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
